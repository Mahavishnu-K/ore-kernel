[workspace]
resolver = "2"
members = [
    "ore-common",
    "ore-core",
    "ore-server",
    "ore-cli"
]

# Optimize release builds for speed and small file size!
[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true