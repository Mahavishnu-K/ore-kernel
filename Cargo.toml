[workspace]
resolver = "2"
members = [
    "ore-core",
    "ore-server",
    "ore-cli",
    "ore-sys"
]

[workspace.package]
version = "0.1.0"
edition = "2024"
authors = ["Mahavishnu-K"]
license = "AGPL-3.0-or-later"
repository = "https://github.com/Mahavishnu-K/ore-kernel"
description = "The Operating System for Local Intelligence. ⚙️"

# Optimize release builds for speed
[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
