[package]
name = "ore-common"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] } # For JSON
uuid = { version = "1.0", features = ["v4", "serde"] } # For Process IDs