app_id = "test_agent"
description = "Omni-Language QA Agent"
version = "1.0.0"

[resources]
allowed_models = ["llama3.2:1b"]
max_tokens_per_minute = 10000
gpu_priority = "normal"
json_history = true
stateful_paging = true

[memory_limits]
max_json_tokens = 4024
max_kv_cache_mb = 1024
auto_summarize_on_cap = true

[file_system]
allowed_read_paths = ["./test_path"] # Maps to /workspace/test_path (Read-Only)
allowed_write_paths = ["./test_output"] # Will map to /workspace/test_output

[execution]
can_execute_shell = false
can_execute_wasm = true
allowed_tools = ["rust", "typescript", "javascript", "go", "python", "simple_script", "js_formatter", "linker_test", "crypto_tool", "finance_tool", "data_tool", "iot_tool", "c_uses_rust", "rust_uses_cpp", "zig_uses_c"]
allowed_language_runtimes = ["python"]
max_cpu_instructions = 5_000_000_000