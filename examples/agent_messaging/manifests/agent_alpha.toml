app_id = "agent_alpha"
description = "Sender agent for IPC messaging demo"
version = "1.0.0"

[privacy]
enforce_pii_redaction = true

[resources]
allowed_models = ["qwen2.5:0.5b"]
max_tokens_per_minute = 10000
gpu_priority = "normal"

[ipc]
allowed_agent_targets = ["agent_beta"]
